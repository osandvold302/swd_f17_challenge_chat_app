import java.io.Serializable;

/** this is the general class that all messages inherit from*/
public class Message implements Serializable{
    // it implements Serializable to send messages through streams
}
